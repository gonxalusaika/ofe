<%= render 'editor' %>
<%= javascript_tag "var recorrido = #{@recorrido.to_json(include: {:estacions => {include: :preguntas}}) };
										var dinosaurios = #{@dinosaurios.to_json()};
										var preguntas = #{@preguntas.to_json(include: :respuestas)}" %>